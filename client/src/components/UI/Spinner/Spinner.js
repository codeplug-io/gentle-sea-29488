import React from "react";
// import classes from "./spinner-classes.css";
import "./spinner-classes.css";

const spinner = props => {
  return <div className="Loader">Loading...</div>;
};

export default spinner;
